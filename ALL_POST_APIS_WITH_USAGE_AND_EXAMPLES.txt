ALL POST APIs - Usage Guide with Request Payload and Response Examples
Generated on 2026-05-15 from live backend and connector OpenAPI documents.

Important safety note:
- Only safe authentication/logout POST calls were executed live.
- Most POST APIs here are write operations. Executing every one of them would create, alter, or cancel real Tally data.
- For those write APIs, this file gives live-ready request examples built from your current runtime data and documented response behavior, but they were not auto-executed.

Current live reference values used in examples:
Current voucher API status after live fixes:
- JSON create is live-verified for Journal, Receipt, and Payment through the normal voucher APIs.
- Voucher routes now return a success payload with IMPORTRESULT when Tally accepts the write.
- Some voucher reads/reports still lag behind or filter inconsistently, so success responses may include a verification warning instead of a hard failure.

- Companies: Raja Pvt Ltd, Software Development
- Example group: Bank Accounts
- Example ledger: BOB
- Currency: INR
- Decimal symbol: paise

====================================================================================================
Backend POST APIs
====================================================================================================

1. Backend POST /api/attendance-types
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: payroll-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Present",
  "ATTENDANCETYPE": "Attendance/Production Type"
}

Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



2. Backend POST /api/auth/logout
Purpose: Log out the current authenticated session and revoke the bearer token.
Tags: auth-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:

No request body documented.
Response example / behavior:
{
  "message": "logged_out"
}


3. Backend POST /api/auth/token
Purpose: Authenticate a user/client and receive an access token for later API calls.
Tags: auth-controller
Auth: No bearer token required.
Parameters: none documented
How to use:
- Send username and password in JSON to receive a bearer token and auth cookie.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "username": "admin",
  "password": "admin@123",
  "include_token": true,
  "company": "Software Development"
}
Response example / behavior:
{
  "message": "authenticated",
  "token_type": "Bearer",
  "expires_in": 3600,
  "expires_at": 1778848651,
  "issued_at": 1778845051,
  "user": {
    "username": "admin",
    "displayName": "Administrator",
    "roles": [
      "ADMIN"
    ]
  },
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IlRBTExZVE9LRU4ifQ..yrY290_zW7viTRyxUicUasq3MtXKsg3pu0vLjmncpDY",
  "claims": {
    "sub": "admin",
    "iss": "tally-backend",
    "aud": "tally-api",
    "iat": 1778845051,
    "exp": 1778848651,
    "jti": "86e17adb00064320bc9811147d76e013",
    "scope": "backend-api",
    "name": "Administrator",
    "roles": [
      "ADMIN"
    ]
  }
}



4. Backend POST /api/auth/users
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: auth-users-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "username": "api-user",
  "displayName": "API User",
  "password": "StrongPass123!",
  "roles": [
    "ADMIN"
  ]
}
Response example / behavior:
{}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


5. Backend POST /api/boms
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API BOM",
  "STOCKITEMNAME": "Sample Item",
  "QUANTITY": "1 Nos"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



6. Backend POST /api/budgets
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "FY26 Budget",
  "FROMDATE": "20260401",
  "TODATE": "20270331"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.




7. Backend POST /api/companies
Purpose: Create, alter, cancel, or delete company master records through the configured action.
Tags: companies-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Software Development",
  "MAILINGNAME": "Software Development",
  "COUNTRYNAME": "India"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.




8. Backend POST /api/companies/alter
Purpose: Update company master data in Tally.
Tags: companies-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Software Development",
  "MAILINGNAME": "Software Development",
  "COUNTRYNAME": "India"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



9. Backend POST /api/companies/create
Purpose: Create a new company in Tally.
Tags: companies-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Demo Company",
  "BOOKSFROM": "20260401",
  "COUNTRYNAME": "India",
  "STATENAME": "Tamil Nadu"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



10. Backend POST /api/companies/open
Purpose: Open an existing company in Tally so later operations target that company.
Tags: companies-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "COMPANYNAME": "Software Development"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



11. Backend POST /api/companies/update
Purpose: Update company master data in Tally.
Tags: companies-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Software Development",
  "MAILINGNAME": "Software Development",
  "COUNTRYNAME": "India"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


12. Backend POST /api/connectors
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: connectors-api-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "connectorId": "local-connector",
  "name": "Local Tally Connector",
  "baseUrl": "http://127.0.0.1:8082"
}
Response example / behavior:
{}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


13. Backend POST /api/connectors/{connectorId}/deactivate
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: connectors-api-controller
Auth: Bearer token required.
Parameters:
- path `connectorId` (required): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


14. Backend POST /api/cost-categories
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Cost Category"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


15. Backend POST /api/cost-centres
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Cost Centre",
  "CATEGORY": "Primary Cost Category"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


16. Backend POST /api/currencies
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "INR",
  "SYMBOL": "INR",
  "DECIMALSYMBOL": "paise"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


17. Backend POST /api/employee-groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: payroll-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Employee Group",
  "PARENT": "Primary"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


18. Backend POST /api/employees
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: payroll-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Employee",
  "PARENT": "Primary"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


19. Backend POST /api/godowns
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Main Godown",
  "PARENT": "Primary"
}


Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

20. Backend POST /api/groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Group",
  "PARENT": "Current Assets"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



21. Backend POST /api/ledger-groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Ledger Group",
  "PARENT": "Current Assets"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.




22. Backend POST /api/ledgers
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Ledger",
  "PARENT": "Bank Accounts",
  "OPENINGBALANCE": "0"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



23. Backend POST /api/pay-heads
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: payroll-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Basic Pay",
  "PAYHEADTYPE": "Earnings for Employees"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

24. Backend POST /api/price-levels
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Retail"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

25. Backend POST /api/price-lists
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "PRICELEVEL": "Retail",
  "STOCKITEMNAME": "Sample Item",
  "RATE": "100.00"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

26. Backend POST /api/projects
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Project",
  "PARENT": "Primary Cost Category"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

27. Backend POST /api/routing/connector
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: routing-api-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "connectorId": "local-connector",
  "company": "Software Development"
}
Response example / behavior:
{}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

28. Backend POST /api/settings/company-currency
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "CURRENCYNAME": "?",
  "MAILINGNAME": "INR",
  "DECIMALSYMBOL": "paise"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

29. Backend POST /api/settings/company-features
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "EMAIL": "sofware@gmail.com",
  "PINCODE": "605001",
  "COUNTRYNAME": "India",
  "STATENAME": "Tamil Nadu",
  "ISINVENTORYON": "Yes",
  "ISGSTON": "Yes"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

30. Backend POST /api/settings/einvoice
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "ENABLED": "Yes",
  "APPLICABLEFROM": "20260515"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

31. Backend POST /api/settings/ewaybill
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "ENABLED": "Yes",
  "APPLICABLEFROM": "20260515"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

32. Backend POST /api/settings/gst-registration
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "GSTIN": "33ABCDE1234F1Z5",
  "STATE": "Tamil Nadu"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

33. Backend POST /api/settings/numbering-rules
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "VOUCHERTYPE": "Sales",
  "PREFIX": "SAL",
  "STARTINGNO": "1001"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

34. Backend POST /api/settings/price-structures
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "NAME": "Default Price Structure",
  "BASEDON": "Stock Item"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



35. Backend POST /api/settings/security-roles
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "NAME": "API Role",
  "ALLOWBACKDATEDVOUCHERS": "Yes"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

36. Backend POST /api/settings/stock-controls
Purpose: Update configuration/settings data in Tally for the selected company.
Tags: settings-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "NEGATIVESTOCK": "No",
  "BATCHWISEDETAILS": "Yes"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

37. Backend POST /api/stock-categories
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Stock Category",
  "PARENT": "Primary"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

38. Backend POST /api/stock-groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Stock Group",
  "PARENT": "Primary"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

39. Backend POST /api/stock-items
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Item",
  "PARENT": "Primary",
  "BASEUNITS": "Nos"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


40. Backend POST /api/sync/attendance-type/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

41. Backend POST /api/sync/attendance/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



42. Backend POST /api/sync/bom/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.


43. Backend POST /api/sync/budget/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

44. Backend POST /api/sync/cancel-invoice/{invoiceId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `invoiceId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

45. Backend POST /api/sync/contra/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

46. Backend POST /api/sync/cost-category/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

47. Backend POST /api/sync/cost-centre/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

48. Backend POST /api/sync/credit-note/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

49. Backend POST /api/sync/customer/{customerId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `customerId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

50. Backend POST /api/sync/debit-note/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

51. Backend POST /api/sync/delivery-note/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

52. Backend POST /api/sync/employee-group/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

53. Backend POST /api/sync/employee/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

54. Backend POST /api/sync/godown/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

55. Backend POST /api/sync/goods-receipt/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

56. Backend POST /api/sync/job-work-in-order/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

57. Backend POST /api/sync/job-work-out-order/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

58. Backend POST /api/sync/journal/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

59. Backend POST /api/sync/material-in/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

60. Backend POST /api/sync/material-out/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

61. Backend POST /api/sync/memorandum/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

62. Backend POST /api/sync/pay-head/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

63. Backend POST /api/sync/payment/{paymentId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `paymentId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

64. Backend POST /api/sync/payroll/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

65. Backend POST /api/sync/physical-stock/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

66. Backend POST /api/sync/price-level/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

67. Backend POST /api/sync/product/{productId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `productId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

68. Backend POST /api/sync/purchase-invoice/{invoiceId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `invoiceId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

69. Backend POST /api/sync/purchase-order/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

70. Backend POST /api/sync/refund/{paymentId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `paymentId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

71. Backend POST /api/sync/rejection-in/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

72. Backend POST /api/sync/rejection-out/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

73. Backend POST /api/sync/return-invoice/{invoiceId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `invoiceId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

74. Backend POST /api/sync/reversing-journal/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

75. Backend POST /api/sync/sales-invoice/{invoiceId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `invoiceId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

76. Backend POST /api/sync/sales-order/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

77. Backend POST /api/sync/stock-category/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

78. Backend POST /api/sync/stock-group/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

79. Backend POST /api/sync/stock-journal/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

80. Backend POST /api/sync/supplier/{supplierId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `supplierId` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

81. Backend POST /api/sync/voucher-type/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Sync APIs
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
- query `forceSync` (optional): boolean
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

82. Backend POST /api/tally
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: connector-proxy-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
sample-text
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

83. Backend POST /api/tally-mappings/ledgers
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Ledger Mapping
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Ledger",
  "PARENT": "Bank Accounts",
  "OPENINGBALANCE": "0"
}
Response example / behavior:
{
  "id": 1,
  "businessUnitId": 1,
  "erpLedgerType": "sample-text",
  "erpLedgerName": "Sample Name",
  "tallyLedgerName": "Sample Name",
  "tallyGroupName": "Sample Name",
  "active": true,
  "createdAt": "sample-text"
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

84. Backend POST /api/tally-mappings/ledgers/defaults/{businessUnitId}
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Ledger Mapping
Auth: Bearer token required.
Parameters:
- path `businessUnitId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
{
  "NAME": "API Test Ledger",
  "PARENT": "Bank Accounts",
  "OPENINGBALANCE": "0"
}
Response example / behavior:
[
  {
    "id": 1,
    "businessUnitId": 1,
    "erpLedgerType": "sample-text",
    "erpLedgerName": "Sample Name",
    "tallyLedgerName": "Sample Name",
    "tallyGroupName": "Sample Name",
    "active": true,
    "createdAt": "sample-text"
  }
]
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

85. Backend POST /api/tally-sync/retry-failed
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Sync Logs
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

86. Backend POST /api/tally-sync/retry/{syncLogId}
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Sync Logs
Auth: Bearer token required.
Parameters:
- path `syncLogId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "success": true,
  "status": "sample-text",
  "message": "sample-text",
  "entityType": "sample-text",
  "entityId": 1,
  "syncLogId": 1,
  "tallyGuid": "sample-text",
  "errors": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

87. Backend POST /api/tally/
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: connector-proxy-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
sample-text
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

88. Backend POST /api/tally/**
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: connector-proxy-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
sample-text
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

89. Backend POST /api/tally/bulk-sync/all-masters
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

90. Backend POST /api/tally/bulk-sync/attendance-types
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Present",
  "ATTENDANCETYPE": "Attendance/Production Type"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

91. Backend POST /api/tally/bulk-sync/attendance-vouchers
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

92. Backend POST /api/tally/bulk-sync/boms
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API BOM",
  "STOCKITEMNAME": "Sample Item",
  "QUANTITY": "1 Nos"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

93. Backend POST /api/tally/bulk-sync/budgets
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "FY26 Budget",
  "FROMDATE": "20260401",
  "TODATE": "20270331"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

94. Backend POST /api/tally/bulk-sync/contras
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

95. Backend POST /api/tally/bulk-sync/cost-categories
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Cost Category"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

96. Backend POST /api/tally/bulk-sync/cost-centres
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Cost Centre",
  "CATEGORY": "Primary Cost Category"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

97. Backend POST /api/tally/bulk-sync/credit-notes
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

98. Backend POST /api/tally/bulk-sync/customers
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

99. Backend POST /api/tally/bulk-sync/debit-notes
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

100. Backend POST /api/tally/bulk-sync/delivery-notes
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

101. Backend POST /api/tally/bulk-sync/employee-groups
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Employee Group",
  "PARENT": "Primary"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

102. Backend POST /api/tally/bulk-sync/employees
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Employee",
  "PARENT": "Primary"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

103. Backend POST /api/tally/bulk-sync/godowns
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Main Godown",
  "PARENT": "Primary"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

104. Backend POST /api/tally/bulk-sync/goods-receipts
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

105. Backend POST /api/tally/bulk-sync/job-work-in-orders
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

106. Backend POST /api/tally/bulk-sync/job-work-out-orders
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

107. Backend POST /api/tally/bulk-sync/journals
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

108. Backend POST /api/tally/bulk-sync/material-ins
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

109. Backend POST /api/tally/bulk-sync/material-outs
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

110. Backend POST /api/tally/bulk-sync/memorandums
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

111. Backend POST /api/tally/bulk-sync/pay-heads
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Basic Pay",
  "PAYHEADTYPE": "Earnings for Employees"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

112. Backend POST /api/tally/bulk-sync/payments
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

113. Backend POST /api/tally/bulk-sync/payroll
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

114. Backend POST /api/tally/bulk-sync/physical-stocks
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

115. Backend POST /api/tally/bulk-sync/price-levels
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Retail"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

116. Backend POST /api/tally/bulk-sync/products
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

117. Backend POST /api/tally/bulk-sync/purchase-invoices
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

118. Backend POST /api/tally/bulk-sync/purchase-orders
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

119. Backend POST /api/tally/bulk-sync/rejections-in
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

120. Backend POST /api/tally/bulk-sync/rejections-out
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

121. Backend POST /api/tally/bulk-sync/reversing-journals
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

122. Backend POST /api/tally/bulk-sync/sales-invoices
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

123. Backend POST /api/tally/bulk-sync/sales-orders
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

124. Backend POST /api/tally/bulk-sync/stock-categories
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Stock Category",
  "PARENT": "Primary"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

125. Backend POST /api/tally/bulk-sync/stock-groups
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Stock Group",
  "PARENT": "Primary"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

126. Backend POST /api/tally/bulk-sync/stock-journals
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

127. Backend POST /api/tally/bulk-sync/suppliers
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "fromDate": "20260515",
  "toDate": "20260515",
  "businessUnitId": 1,
  "skipAlreadySynced": true,
  "forceSync": true
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

128. Backend POST /api/tally/bulk-sync/voucher-types
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Bulk Sync
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Sales",
  "PARENT": "Sales"
}
Response example / behavior:
{
  "total": 1,
  "success": 1,
  "failed": 1,
  "skipped": 1,
  "failedItems": [
    "<nested>"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

129. Backend POST /api/tally/company/select
Purpose: Forward a generic POST request from the backend to the connector/Tally layer.
Tags: Tally Company Context
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "companyName": "Sample Name"
}
Response example / behavior:
{}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

130. Backend POST /api/tally/validate/attendance-type/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

131. Backend POST /api/tally/validate/bom/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

132. Backend POST /api/tally/validate/budget/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

133. Backend POST /api/tally/validate/cost-category/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

134. Backend POST /api/tally/validate/cost-centre/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

135. Backend POST /api/tally/validate/customer/{customerId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `customerId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

136. Backend POST /api/tally/validate/employee-group/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

137. Backend POST /api/tally/validate/employee/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

138. Backend POST /api/tally/validate/godown/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

139. Backend POST /api/tally/validate/pay-head/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

140. Backend POST /api/tally/validate/payment/{paymentId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `paymentId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

141. Backend POST /api/tally/validate/price-level/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

142. Backend POST /api/tally/validate/product/{productId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `productId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

143. Backend POST /api/tally/validate/purchase-invoice/{invoiceId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `invoiceId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

144. Backend POST /api/tally/validate/sales-invoice/{invoiceId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `invoiceId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

145. Backend POST /api/tally/validate/stock-category/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

146. Backend POST /api/tally/validate/stock-group/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

147. Backend POST /api/tally/validate/supplier/{supplierId}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `supplierId` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

148. Backend POST /api/tally/validate/voucher-type/{id}
Purpose: Run backend sync or validation logic for an application entity before/while sending it to Tally.
Tags: Tally Validation
Auth: Bearer token required.
Parameters:
- path `id` (required): integer
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- This POST call mainly uses path/query/header inputs; the request body is optional or not documented.
Request body example:
No request body documented.
Response example / behavior:
{
  "valid": true,
  "errors": [
    "sample-text"
  ],
  "warnings": [
    "sample-text"
  ]
}
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

149. Backend POST /api/uoms
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Nos",
  "SYMBOL": "Nos"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

150. Backend POST /api/voucher-types
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Tags: master-data-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Sales",
  "PARENT": "Sales"
}
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.



151. Backend POST /api/vouchers
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Generic Voucher create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "generic voucher voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

152. Backend POST /api/vouchers/attendance
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

153. Backend POST /api/vouchers/contra
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- This accounting voucher family uses the same corrected create path as Journal/Receipt/Payment.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "contra voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: updated after live voucher fixes. This route now uses the corrected voucher import shape; verify the created voucher in Tally if the response includes `_connector_warning`.

154. Backend POST /api/vouchers/credit-notes
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- This accounting voucher family uses the same corrected create path as Journal/Receipt/Payment.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "credit note voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: updated after live voucher fixes. This route now uses the corrected voucher import shape; verify the created voucher in Tally if the response includes `_connector_warning`.

155. Backend POST /api/vouchers/debit-notes
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- This accounting voucher family uses the same corrected create path as Journal/Receipt/Payment.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "debit note voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: updated after live voucher fixes. This route now uses the corrected voucher import shape; verify the created voucher in Tally if the response includes `_connector_warning`.

156. Backend POST /api/vouchers/delivery-notes
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

157. Backend POST /api/vouchers/einvoice-ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

158. Backend POST /api/vouchers/einvoice-ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

159. Backend POST /api/vouchers/einvoice-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

160. Backend POST /api/vouchers/einvoice/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

161. Backend POST /api/vouchers/ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

162. Backend POST /api/vouchers/ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

163. Backend POST /api/vouchers/goods-receipts
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

164. Backend POST /api/vouchers/import-xml
Purpose: Send raw Tally XML import content directly to the connector or backend proxy.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use this route when you want the most reliable voucher write path in the current build.
- Send XML and include `<SVCURRENTCOMPANY>` for the target company.
- XML import was live-tested successfully for voucher creation in this environment.
Request body example:
<ENVELOPE>
<HEADER>
<VERSION>1</VERSION>
<TALLYREQUEST>Import</TALLYREQUEST>
<TYPE>Data</TYPE>
<ID>Vouchers</ID>
</HEADER>
<BODY>
<DESC>
<STATICVARIABLES>
<SVCURRENTCOMPANY>Software Development</SVCURRENTCOMPANY>
</STATICVARIABLES>
</DESC>
<DATA>
<TALLYMESSAGE>
<VOUCHER VCHTYPE="Journal" ACTION="Create">
<DATE>20260402</DATE>
<NARRATION>Journal created from API test</NARRATION>
<VOUCHERTYPENAME>Journal</VOUCHERTYPENAME>
<EFFECTIVEDATE>20260402</EFFECTIVEDATE>
<ALLLEDGERENTRIES.LIST>
<LEDGERNAME>BOB</LEDGERNAME>
<AMOUNT>100.00</AMOUNT>
</ALLLEDGERENTRIES.LIST>
<ALLLEDGERENTRIES.LIST>
<LEDGERNAME>Cash</LEDGERNAME>
<ISDEEMEDPOSITIVE>Yes</ISDEEMEDPOSITIVE>
<AMOUNT>-100.00</AMOUNT>
</ALLLEDGERENTRIES.LIST>
</VOUCHER>
</TALLYMESSAGE>
</DATA>
</BODY>
</ENVELOPE>
Response example / behavior:
{
  "ENVELOPE": {
    "BODY": {
      "DATA": {
        "IMPORTRESULT": {
          "CREATED": "1",
          "ALTERED": "0",
          "DELETED": "0",
          "EXCEPTIONS": "0"
        }
      }
    }
  }
}
Execution note: live-tested successfully. This XML route created real vouchers in Tally and returned CREATED=1.

165. Backend POST /api/vouchers/job-work-in-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

166. Backend POST /api/vouchers/job-work-out-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

167. Backend POST /api/vouchers/journal
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Journal create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "9020"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "journal voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

168. Backend POST /api/vouchers/manufacturing
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

169. Backend POST /api/vouchers/material-in
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

170. Backend POST /api/vouchers/material-out
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

171. Backend POST /api/vouchers/memorandum
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

172. Backend POST /api/vouchers/payment
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Payment create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Payment",
  "VOUCHERNUMBER": "9022",
  "NARRATION": "Payment created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "Cash",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "BOB",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "9022"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "payment voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

173. Backend POST /api/vouchers/payroll
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

174. Backend POST /api/vouchers/physical-stock
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

175. Backend POST /api/vouchers/purchase
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

176. Backend POST /api/vouchers/purchase-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

177. Backend POST /api/vouchers/receipt
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Receipt create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Receipt",
  "VOUCHERNUMBER": "9021",
  "NARRATION": "Receipt created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "9021"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "receipt voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

178. Backend POST /api/vouchers/rejections-in
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

179. Backend POST /api/vouchers/rejections-out
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

180. Backend POST /api/vouchers/reversing-journal
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

181. Backend POST /api/vouchers/sales
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

182. Backend POST /api/vouchers/sales-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

183. Backend POST /api/vouchers/sales/einvoice-ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

184. Backend POST /api/vouchers/sales/einvoice-ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

185. Backend POST /api/vouchers/sales/einvoice-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

186. Backend POST /api/vouchers/sales/einvoice/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

187. Backend POST /api/vouchers/sales/ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

188. Backend POST /api/vouchers/sales/ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

189. Backend POST /api/vouchers/stock-journals
Purpose: Create, alter, or submit voucher transactions to Tally.
Tags: vouchers-api-controller
Auth: Bearer token required.
Parameters:
- query `action` (optional): string
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send `company=Software Development` or another open company name; the backend now forwards that safely to the connector.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

190. Backend POST /api/xml/execute
Purpose: Execute raw Tally XML envelopes directly against the connector for advanced custom operations.
Tags: xml-api-controller
Auth: Bearer token required.
Parameters: none documented
How to use:
- Authenticate first with POST /api/auth/token and send `Authorization: Bearer <token>` in later calls.
- Send a complete Tally XML envelope in the request body; use XML media types when possible.
Request body example:
<ENVELOPE><HEADER><TALLYREQUEST>Export</TALLYREQUEST></HEADER><BODY></BODY></ENVELOPE>
Response example / behavior:
<raw connector/Tally response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

Total Backend POST endpoints documented here: 190

====================================================================================================
Connector POST APIs
====================================================================================================

1. Connector POST /attendance-types
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Present",
  "ATTENDANCETYPE": "Attendance/Production Type"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

2. Connector POST /auth/token
Purpose: Authenticate a user/client and receive an access token for later API calls.
Doc Summary: Auth Token
Auth: No bearer token required; connector-specific auth rules apply.
Parameters: none documented
How to use:
- Send connector client credentials in JSON if connector auth is enabled for your deployment.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "username": "admin",
  "password": "admin@123",
  "include_token": true,
  "company": "Software Development"
}
Response example / behavior:
{
  "detail": "invalid_client"
}

3. Connector POST /boms
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API BOM",
  "STOCKITEMNAME": "Sample Item",
  "QUANTITY": "1 Nos"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

4. Connector POST /budgets
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "FY26 Budget",
  "FROMDATE": "20260401",
  "TODATE": "20270331"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

5. Connector POST /companies
Purpose: Create, alter, cancel, or delete company master records through the configured action.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Software Development",
  "MAILINGNAME": "Software Development",
  "COUNTRYNAME": "India"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

6. Connector POST /companies/alter
Purpose: Update company master data in Tally.
Doc Summary: Company Alter
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Software Development",
  "MAILINGNAME": "Software Development",
  "COUNTRYNAME": "India"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

7. Connector POST /companies/create
Purpose: Create a new company in Tally.
Doc Summary: Company Create
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Demo Company",
  "BOOKSFROM": "20260401",
  "COUNTRYNAME": "India",
  "STATENAME": "Tamil Nadu"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

8. Connector POST /companies/open
Purpose: Open an existing company in Tally so later operations target that company.
Doc Summary: Company Open
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "COMPANYNAME": "Software Development"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

9. Connector POST /companies/update
Purpose: Update company master data in Tally.
Doc Summary: Company Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Software Development",
  "MAILINGNAME": "Software Development",
  "COUNTRYNAME": "India"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

10. Connector POST /cost-categories
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Cost Category"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

11. Connector POST /cost-centres
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Cost Centre",
  "CATEGORY": "Primary Cost Category"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

12. Connector POST /currencies
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "INR",
  "SYMBOL": "INR",
  "DECIMALSYMBOL": "paise"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

13. Connector POST /employee-groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Employee Groups Upsert
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Employee Group",
  "PARENT": "Primary"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

14. Connector POST /employees
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Employees Upsert
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Employee",
  "PARENT": "Primary"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

15. Connector POST /godowns
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Main Godown",
  "PARENT": "Primary"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

16. Connector POST /groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Group",
  "PARENT": "Current Assets"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

17. Connector POST /ledger-groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Ledger Group",
  "PARENT": "Current Assets"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

18. Connector POST /ledgers
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Ledger",
  "PARENT": "Bank Accounts",
  "OPENINGBALANCE": "0"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

19. Connector POST /pay-heads
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Basic Pay",
  "PAYHEADTYPE": "Earnings for Employees"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

20. Connector POST /price-levels
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Retail"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

21. Connector POST /price-lists
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Price Lists Upsert
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "PRICELEVEL": "Retail",
  "STOCKITEMNAME": "Sample Item",
  "RATE": "100.00"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

22. Connector POST /projects
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Project",
  "PARENT": "Primary Cost Category"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

23. Connector POST /settings/company-currency
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Company Currency Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "CURRENCYNAME": "?",
  "MAILINGNAME": "INR",
  "DECIMALSYMBOL": "paise"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

24. Connector POST /settings/company-features
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Company Features Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "EMAIL": "sofware@gmail.com",
  "PINCODE": "605001",
  "COUNTRYNAME": "India",
  "STATENAME": "Tamil Nadu",
  "ISINVENTORYON": "Yes",
  "ISGSTON": "Yes"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

25. Connector POST /settings/einvoice
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Einvoice Settings Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "ENABLED": "Yes",
  "APPLICABLEFROM": "20260515"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

26. Connector POST /settings/ewaybill
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Ewaybill Settings Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "ENABLED": "Yes",
  "APPLICABLEFROM": "20260515"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

27. Connector POST /settings/gst-registration
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Gst Registration Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "GSTIN": "33ABCDE1234F1Z5",
  "STATE": "Tamil Nadu"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

28. Connector POST /settings/numbering-rules
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Numbering Rules Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "VOUCHERTYPE": "Sales",
  "PREFIX": "SAL",
  "STARTINGNO": "1001"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

29. Connector POST /settings/price-structures
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Price Structures Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "NAME": "Default Price Structure",
  "BASEDON": "Stock Item"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

30. Connector POST /settings/security-roles
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Security Roles Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "NAME": "API Role",
  "ALLOWBACKDATEDVOUCHERS": "Yes"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

31. Connector POST /settings/stock-controls
Purpose: Update configuration/settings data in Tally for the selected company.
Doc Summary: Stock Controls Update
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
- Use current company values as a base, then change only the fields you really want to update.
Request body example:
{
  "NEGATIVESTOCK": "No",
  "BATCHWISEDETAILS": "Yes"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

32. Connector POST /stock-categories
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Stock Category",
  "PARENT": "Primary"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

33. Connector POST /stock-groups
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Stock Group",
  "PARENT": "Primary"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

34. Connector POST /stock-items
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "API Test Item",
  "PARENT": "Primary",
  "BASEUNITS": "Nos"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

35. Connector POST /uoms
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Nos",
  "SYMBOL": "Nos"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

36. Connector POST /voucher-types
Purpose: Create, alter, cancel, or delete master data in Tally through this endpoint.
Doc Summary: Upsert Master
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `company` (optional): string / null
- query `action` (optional): string / null
- header `x-agent-key` (optional): string / null
- header `x-company` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Send JSON in the request body that matches the expected master, voucher, or settings fields.
Request body example:
{
  "NAME": "Sales",
  "PARENT": "Sales"
}
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

37. Connector POST /vouchers
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Voucher Upsert
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Generic Voucher create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "generic voucher voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

38. Connector POST /vouchers/attendance
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

39. Connector POST /vouchers/contra
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- This accounting voucher family uses the same corrected create path as Journal/Receipt/Payment.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "contra voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: updated after live voucher fixes. This route now uses the corrected voucher import shape; verify the created voucher in Tally if the response includes `_connector_warning`.

40. Connector POST /vouchers/credit-notes
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- This accounting voucher family uses the same corrected create path as Journal/Receipt/Payment.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "credit note voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: updated after live voucher fixes. This route now uses the corrected voucher import shape; verify the created voucher in Tally if the response includes `_connector_warning`.

41. Connector POST /vouchers/debit-notes
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- This accounting voucher family uses the same corrected create path as Journal/Receipt/Payment.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "AUTO-1001"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "debit note voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: updated after live voucher fixes. This route now uses the corrected voucher import shape; verify the created voucher in Tally if the response includes `_connector_warning`.

42. Connector POST /vouchers/delivery-notes
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

43. Connector POST /vouchers/einvoice-ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Voucher Einvoice Ewaybill Ready
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

44. Connector POST /vouchers/einvoice-ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Voucher Einvoice Ewaybill Generate
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

45. Connector POST /vouchers/einvoice-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Voucher Einvoice Ready
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

46. Connector POST /vouchers/einvoice/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Voucher Einvoice Generate
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

47. Connector POST /vouchers/ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Voucher Ewaybill Ready
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

48. Connector POST /vouchers/ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Voucher Ewaybill Generate
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `voucher_type` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

49. Connector POST /vouchers/goods-receipts
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

50. Connector POST /vouchers/import-xml
Purpose: Send raw Tally XML import content directly to the connector or backend proxy.
Doc Summary: Vouchers Import Xml
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use XML import when you need the closest possible native Tally write path.
- XML import was live-tested successfully for voucher creation in this environment.
Request body example:
<ENVELOPE>
<HEADER>
<VERSION>1</VERSION>
<TALLYREQUEST>Import</TALLYREQUEST>
<TYPE>Data</TYPE>
<ID>Vouchers</ID>
</HEADER>
<BODY>
<DESC>
<STATICVARIABLES>
<SVCURRENTCOMPANY>Software Development</SVCURRENTCOMPANY>
</STATICVARIABLES>
</DESC>
<DATA>
<TALLYMESSAGE>
<VOUCHER VCHTYPE="Journal" ACTION="Create">
<DATE>20260402</DATE>
<NARRATION>Journal created from API test</NARRATION>
<VOUCHERTYPENAME>Journal</VOUCHERTYPENAME>
<EFFECTIVEDATE>20260402</EFFECTIVEDATE>
<ALLLEDGERENTRIES.LIST>
<LEDGERNAME>BOB</LEDGERNAME>
<AMOUNT>100.00</AMOUNT>
</ALLLEDGERENTRIES.LIST>
<ALLLEDGERENTRIES.LIST>
<LEDGERNAME>Cash</LEDGERNAME>
<ISDEEMEDPOSITIVE>Yes</ISDEEMEDPOSITIVE>
<AMOUNT>-100.00</AMOUNT>
</ALLLEDGERENTRIES.LIST>
</VOUCHER>
</TALLYMESSAGE>
</DATA>
</BODY>
</ENVELOPE>
Response example / behavior:
{
  "ENVELOPE": {
    "BODY": {
      "DATA": {
        "IMPORTRESULT": {
          "CREATED": "1",
          "ALTERED": "0",
          "DELETED": "0",
          "EXCEPTIONS": "0"
        }
      }
    }
  }
}
Execution note: live-tested successfully. This XML route created real vouchers in Tally and returned CREATED=1.

51. Connector POST /vouchers/job-work-in-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

52. Connector POST /vouchers/job-work-out-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

53. Connector POST /vouchers/journal
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Journal create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "9020"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "journal voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

54. Connector POST /vouchers/manufacturing
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

55. Connector POST /vouchers/material-in
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

56. Connector POST /vouchers/material-out
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

57. Connector POST /vouchers/memorandum
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

58. Connector POST /vouchers/payment
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Payment create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Payment",
  "VOUCHERNUMBER": "9022",
  "NARRATION": "Payment created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "Cash",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "BOB",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "9022"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "payment voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

59. Connector POST /vouchers/payroll
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

60. Connector POST /vouchers/physical-stock
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

61. Connector POST /vouchers/purchase
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

62. Connector POST /vouchers/purchase-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

63. Connector POST /vouchers/receipt
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use a balanced `LEDGERENTRIES.LIST` payload. The connector now maps accounting vouchers into a Tally-accepted write shape.
- Receipt create was live-tested after the fix and returned `CREATED=1`.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Receipt",
  "VOUCHERNUMBER": "9021",
  "NARRATION": "Receipt created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
{
  "IMPORTRESULT": {
    "CREATED": "1",
    "ALTERED": "0",
    "DELETED": "0",
    "LASTVCHID": "6",
    "LASTMID": "0",
    "COMBINED": "0",
    "IGNORED": "0",
    "ERRORS": "0",
    "CANCELLED": "0",
    "EXCEPTIONS": "0",
    "VCHNUMBER": "9021"
  },
  "_connector_warning": {
    "supported": true,
    "resource": "receipt voucher",
    "fallback_applied": true,
    "error_type": "write_visibility_unverified",
    "reason": "Tally acknowledged the voucher write, but the connector could not immediately verify it through Day Book export. Confirm it in Tally or re-query after the report context refreshes."
  }
}
Execution note: live-tested successfully after the voucher fix. The response may still include `_connector_warning` when Day Book verification lags behind the import acknowledgement.

64. Connector POST /vouchers/rejections-in
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

65. Connector POST /vouchers/rejections-out
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

66. Connector POST /vouchers/reversing-journal
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

67. Connector POST /vouchers/sales
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

68. Connector POST /vouchers/sales-orders
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

69. Connector POST /vouchers/sales/einvoice-ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Sales Einvoice Ewaybill Ready
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

70. Connector POST /vouchers/sales/einvoice-ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Sales Einvoice Ewaybill Generate
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

71. Connector POST /vouchers/sales/einvoice-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Sales Einvoice Ready
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

72. Connector POST /vouchers/sales/einvoice/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Sales Einvoice Generate
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

73. Connector POST /vouchers/sales/ewaybill-ready
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Sales Ewaybill Ready
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

74. Connector POST /vouchers/sales/ewaybill/generate
Purpose: Trigger e-invoice / e-way bill related processing for voucher data.
Doc Summary: Sales Ewaybill Generate
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- These compliance routes depend on valid Sales voucher context and additional e-invoice/e-way bill fields.
Request body example:
{
  "VOUCHERNUMBER": "AUTO-1001",
  "VOUCHERTYPENAME": "Sales",
  "DATE": "20260402"
}
Response example / behavior:
<connector/Tally compliance response. These routes are separate from the base voucher create fix and still depend on sales/compliance-specific data.>
Execution note: documented example only. The base voucher create fix does not automatically prove every compliance route.

75. Connector POST /vouchers/stock-journals
Purpose: Create, alter, or submit voucher transactions to Tally.
Doc Summary: Upsert Voucher
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `action` (optional): string / null
- query `company` (optional): string / null
- header `x-company` (optional): string / null
- header `x-agent-key` (optional): string / null
How to use:
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Use the `action` query parameter to choose `Create`, `Alter`, `Cancel`, or `Delete` when supported.
- Use `company=Software Development` or send `x-company: Software Development` to target a specific company.
- Use `DATE`, `VOUCHERTYPENAME`, and a balanced `LEDGERENTRIES.LIST` or inventory structure depending on the voucher family.
Request body example:
{
  "DATE": "20260402",
  "VOUCHERTYPENAME": "Journal",
  "VOUCHERNUMBER": "9020",
  "NARRATION": "Journal created via fixed JSON voucher API",
  "LEDGERENTRIES.LIST": [
    {
      "LEDGERNAME": "BOB",
      "AMOUNT": "100.00"
    },
    {
      "LEDGERNAME": "Cash",
      "ISDEEMEDPOSITIVE": "Yes",
      "AMOUNT": "-100.00"
    }
  ]
}
Response example / behavior:
<connector/Tally import response. Use a balanced voucher payload and verify the created voucher in Tally.>
Execution note: updated to the corrected voucher create format. Live create was verified on Journal, Receipt, and Payment; related routes share the same core write path but may still need voucher-specific business fields.

76. Connector POST /xml/execute
Purpose: Execute raw Tally XML envelopes directly against the connector for advanced custom operations.
Doc Summary: Xml Execute
Auth: x-agent-key may be required depending on connector configuration.
Parameters:
- query `view` (optional): string / null
- query `import_mode` (optional): boolean / null
- header `x-agent-key` (optional): string / null
How to use:
- Send the optional `x-agent-key` header if your connector is configured to require it.
- Call the connector directly on port 8082 after the connector is running and TallyPrime is reachable.
- Send a complete Tally XML envelope in the request body; use XML media types when possible.
Request body example:
<ENVELOPE><HEADER><TALLYREQUEST>Export</TALLYREQUEST></HEADER><BODY></BODY></ENVELOPE>
Response example / behavior:
<connector response body>
Execution note: write endpoint not auto-executed; example is prepared from the current live environment plus the documented schema/path behavior.

Total Connector POST endpoints documented here: 76

